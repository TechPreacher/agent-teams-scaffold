# {{REPO_NAME}} — Project Context

> Generated by `agent-teams-scaffold` on {{DATE}}. Review every `TODO` below and fill it in —
> teammates load this file at startup but do **not** inherit the lead's conversation history, so
> this file is their shared ground truth.

## Stack

{{LANGUAGES}}

## Commands

- Build: `{{BUILD_CMD}}`
- Test: `{{TEST_CMD}}`
- Lint: `{{LINT_CMD}}`

## Module boundaries

Teammates use these to avoid editing the same files. Keep scopes disjoint where possible.

{{MODULE_LIST}}

## Verification

A task is not done until `{{TEST_CMD}}` and `{{LINT_CMD}}` pass. Run them before reporting back.

## Conventions

TODO: coding conventions, error-handling patterns, naming, anything a fresh teammate would get
wrong without being told.

## Agent Teams notes

- Experimental teams are enabled via `.claude/settings.json`
  (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`). Requires Claude Code v2.1.32+.
- Ready-to-paste spawn prompts are in `.claude/TEAM_PROMPTS.md`.
- Launch a team session with `.claude/launch-team.fish` (needs tmux 3.2+ for split panes).
- After creating a team, press `Shift+Tab` to put the lead in coordination-only (delegate) mode.
- For 4+ teammates, give each its own git worktree so they don't collide on files.
- Teams cost roughly 3–7× the tokens of a single session — plan first in plan mode, then hand the
  approved plan to the team.
